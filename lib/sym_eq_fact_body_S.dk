def sym_eq_fact_body_S :
  sttfa.eps (sttfa.forall nat.nat (n:(sttfa.etap (sttfa.p nat.nat)) => leibniz.leibniz nat.nat (times.times (fact.fact n) (S.S n)) (fact_body.fact_body (S.S n))))
  :=
  n:(sttfa.etap (sttfa.p nat.nat)) => sym_leibniz.sym_leibniz nat.nat (fact_body.fact_body (S.S n)) (times.times (fact.fact n) (S.S n)) (eq_fact_body_S.eq_fact_body_S n).

