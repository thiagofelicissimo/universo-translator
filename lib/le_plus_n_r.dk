def le_plus_n_r :
  sttfa.eps (sttfa.forall nat.nat (n:(sttfa.etap (sttfa.p nat.nat)) => sttfa.forall nat.nat (m:(sttfa.etap (sttfa.p nat.nat)) => le.le m (plus.plus m n))))
  :=
  n:(sttfa.etap (sttfa.p nat.nat)) => m:(sttfa.etap (sttfa.p nat.nat)) => eq_coerc.eq_coerc (le.le m (plus.plus n m)) (le.le m (plus.plus m n)) (le_plus_n.le_plus_n n m) (rewrite_r.rewrite_r nat.nat (plus.plus n m) (__:(sttfa.etap (sttfa.p nat.nat)) => eq.eq sttfa.bool (le.le m (plus.plus n m)) (le.le m __)) (refl.refl sttfa.bool (le.le m (plus.plus n m))) (plus.plus m n) (commutative_plus.commutative_plus m n)).

